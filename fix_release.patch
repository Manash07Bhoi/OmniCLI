<<<<<<< SEARCH
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Flatten artifacts and generate checksums
        run: |
          cd dist
          find . -mindepth 2 -type f -exec cp -n {} . \; || true
          find . -mindepth 1 -type d -exec rm -rf {} + || true
          for file in *; do
            if [ -f "$file" ] && [ "$file" != "SHA256SUMS.txt" ]; then
              sha256sum "$file" >> SHA256SUMS.txt
            fi
          done
=======
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd dist
          for file in *; do
            if [ -f "$file" ] && [ "$file" != "SHA256SUMS.txt" ]; then
              sha256sum "$file" >> SHA256SUMS.txt
            fi
          done
>>>>>>> REPLACE
