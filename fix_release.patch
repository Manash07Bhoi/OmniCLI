<<<<<<< SEARCH
      - name: Rename artifacts
        run: |
          # Copy to exactly the canonical name to a flat staging dir to enforce 
          # download-artifact@v4 pulling exactly what we expect without any subdirectories
          mkdir -p final_artifacts
          if [ -f "omnicli/omnicli-${{ matrix.target }}.tar.gz" ]; then
            cp "omnicli/omnicli-${{ matrix.target }}.tar.gz" "final_artifacts/omnicli-${{ matrix.target }}.tar.gz"
          fi
          if [ -f "omnicli/omnicli-${{ matrix.target }}.zip" ]; then
            cp "omnicli/omnicli-${{ matrix.target }}.zip" "final_artifacts/omnicli-${{ matrix.target }}.zip"
          fi
=======
      - name: Rename artifacts (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p final_artifacts
          if [ -f "omnicli/omnicli-${{ matrix.target }}.tar.gz" ]; then
            cp "omnicli/omnicli-${{ matrix.target }}.tar.gz" "final_artifacts/omnicli-${{ matrix.target }}.tar.gz"
          fi
          
      - name: Rename artifacts (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path final_artifacts
          if (Test-Path "omnicli/omnicli-${{ matrix.target }}.zip") {
            Copy-Item "omnicli/omnicli-${{ matrix.target }}.zip" "final_artifacts/omnicli-${{ matrix.target }}.zip"
          }
>>>>>>> REPLACE
