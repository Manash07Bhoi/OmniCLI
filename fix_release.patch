<<<<<<< SEARCH
      - name: Flatten artifacts and generate checksums
        run: |
          cd dist
          find . -mindepth 2 -type f -exec mv {} . \; || true
          find . -type d -empty -delete || true
          for file in *; do
            if [ -f "$file" ]; then
              sha256sum "$file" >> SHA256SUMS.txt
            fi
          done
=======
      - name: Flatten artifacts and generate checksums
        run: |
          cd dist
          find . -mindepth 2 -type f -exec mv -n {} . \; || true
          find . -type d -empty -delete || true
          for file in *; do
            if [ -f "$file" ]; then
              sha256sum "$file" >> SHA256SUMS.txt
            fi
          done
>>>>>>> REPLACE
