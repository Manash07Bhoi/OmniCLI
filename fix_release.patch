<<<<<<< SEARCH
      - name: Validate artifacts and generate checksums
        run: |
          cd release_assets
          
          echo "Validating downloaded artifact tree:"
          ls -R
          
          # We no longer use mv/cp here at all since we fixed it on the build upload side using final_artifacts/
          # but previously added mv commands here are still failing somehow? 
          # The logs say: `mv: cannot overwrite directory './omnicli-windows-x86_64.zip' with non-directory`
          # Wait... the error in logs is from a lingering `mv` command. But `mv` is nowhere in the YAML above! 
          
          # Let's ensure checksum generates securely
          echo "Generating checksums for exactly the release artifacts..."
          sha256sum omnicli-* > SHA256SUMS.txt || true
          cat SHA256SUMS.txt || true
=======
      - name: Validate artifacts and generate checksums
        run: |
          cd release_assets
          
          echo "Validating downloaded artifact tree:"
          ls -laR
          
          echo "Generating checksums for exactly the release artifacts..."
          # Secure checksum generation directly on expected release assets
          sha256sum omnicli-* > SHA256SUMS.txt || true
          cat SHA256SUMS.txt || true
>>>>>>> REPLACE
